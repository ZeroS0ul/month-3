-- database: /home/aviasells/Desktop/delta/db/sqlite3.db

-- Чтобы выполнить весь файл, нажмите кнопку ▷ в правом верхнем углу окна.

SELECT * FROM products_detail;

SELECT * FROM products pr 
INNER JOIN products_detail pr2 on pr.product_id = pr2.product_id;


SELECT * FROM products pr 
LEFT JOIN products_detail pr2 on pr.product_id = pr2.product_id;

SELECT * FROM products pr 
RIGHT JOIN products_detail pr2 on pr.product_id = pr2.product_id;

SELECT * FROM products pr 
FULL JOIN products_detail pr2 on pr.product_id = pr2.product_id;